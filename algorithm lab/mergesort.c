#include <stdio.h>

// Function definition
int add(int a, int b) {  
    return a + b;  // Processing and returning the result
}

int main() {
    int result = add(5, 3);  // Calling the function
    printf("Sum: %d", result);  // Output
    return 0;
}
